export type defaultMDX = {
  meta: {
    title: string;
    author: string;
    description: string;
    date: string;
  };
  slug: string;
  content: string;
};

export type CustomMDXProps = {
  meta: {
    title: string;
    author: string;
    description: string;
    date: string;
  };
  slug: string;
  // content: any;
};
